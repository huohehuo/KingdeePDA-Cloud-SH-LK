package com.fangzuo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.fangzuo.assist.cloud.Dao.PDMain;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PDMAIN".
*/
public class PDMainDao extends AbstractDao<PDMain, Long> {

    public static final String TABLENAME = "PDMAIN";

    /**
     * Properties of entity PDMain.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property FID = new Property(1, String.class, "FID", false, "FID");
        public final static Property FBillNo = new Property(2, String.class, "FBillNo", false, "FBILL_NO");
        public final static Property FDate = new Property(3, String.class, "FDate", false, "FDATE");
        public final static Property FSchemeName = new Property(4, String.class, "FSchemeName", false, "FSCHEME_NAME");
        public final static Property FSchemeNo = new Property(5, String.class, "FSchemeNo", false, "FSCHEME_NO");
    }


    public PDMainDao(DaoConfig config) {
        super(config);
    }
    
    public PDMainDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PDMAIN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"FID\" TEXT," + // 1: FID
                "\"FBILL_NO\" TEXT," + // 2: FBillNo
                "\"FDATE\" TEXT," + // 3: FDate
                "\"FSCHEME_NAME\" TEXT," + // 4: FSchemeName
                "\"FSCHEME_NO\" TEXT);"); // 5: FSchemeNo
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PDMAIN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PDMain entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String FID = entity.getFID();
        if (FID != null) {
            stmt.bindString(2, FID);
        }
 
        String FBillNo = entity.getFBillNo();
        if (FBillNo != null) {
            stmt.bindString(3, FBillNo);
        }
 
        String FDate = entity.getFDate();
        if (FDate != null) {
            stmt.bindString(4, FDate);
        }
 
        String FSchemeName = entity.getFSchemeName();
        if (FSchemeName != null) {
            stmt.bindString(5, FSchemeName);
        }
 
        String FSchemeNo = entity.getFSchemeNo();
        if (FSchemeNo != null) {
            stmt.bindString(6, FSchemeNo);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PDMain entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String FID = entity.getFID();
        if (FID != null) {
            stmt.bindString(2, FID);
        }
 
        String FBillNo = entity.getFBillNo();
        if (FBillNo != null) {
            stmt.bindString(3, FBillNo);
        }
 
        String FDate = entity.getFDate();
        if (FDate != null) {
            stmt.bindString(4, FDate);
        }
 
        String FSchemeName = entity.getFSchemeName();
        if (FSchemeName != null) {
            stmt.bindString(5, FSchemeName);
        }
 
        String FSchemeNo = entity.getFSchemeNo();
        if (FSchemeNo != null) {
            stmt.bindString(6, FSchemeNo);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public PDMain readEntity(Cursor cursor, int offset) {
        PDMain entity = new PDMain( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // FID
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // FBillNo
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // FDate
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // FSchemeName
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // FSchemeNo
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PDMain entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFID(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFBillNo(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFDate(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFSchemeName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setFSchemeNo(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PDMain entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PDMain entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PDMain entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
