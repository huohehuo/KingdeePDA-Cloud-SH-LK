package com.fangzuo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.fangzuo.assist.cloud.Dao.Buyer;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BUYER".
*/
public class BuyerDao extends AbstractDao<Buyer, Void> {

    public static final String TABLENAME = "BUYER";

    /**
     * Properties of entity Buyer.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FID = new Property(0, String.class, "FID", false, "FID");
        public final static Property FNumber = new Property(1, String.class, "FNumber", false, "FNUMBER");
        public final static Property FName = new Property(2, String.class, "FName", false, "FNAME");
        public final static Property FDeptID = new Property(3, String.class, "FDeptID", false, "FDEPT_ID");
        public final static Property FOrg = new Property(4, String.class, "FOrg", false, "FORG");
    }


    public BuyerDao(DaoConfig config) {
        super(config);
    }
    
    public BuyerDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BUYER\" (" + //
                "\"FID\" TEXT," + // 0: FID
                "\"FNUMBER\" TEXT," + // 1: FNumber
                "\"FNAME\" TEXT," + // 2: FName
                "\"FDEPT_ID\" TEXT," + // 3: FDeptID
                "\"FORG\" TEXT);"); // 4: FOrg
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BUYER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Buyer entity) {
        stmt.clearBindings();
 
        String FID = entity.getFID();
        if (FID != null) {
            stmt.bindString(1, FID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(2, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(3, FName);
        }
 
        String FDeptID = entity.getFDeptID();
        if (FDeptID != null) {
            stmt.bindString(4, FDeptID);
        }
 
        String FOrg = entity.getFOrg();
        if (FOrg != null) {
            stmt.bindString(5, FOrg);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Buyer entity) {
        stmt.clearBindings();
 
        String FID = entity.getFID();
        if (FID != null) {
            stmt.bindString(1, FID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(2, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(3, FName);
        }
 
        String FDeptID = entity.getFDeptID();
        if (FDeptID != null) {
            stmt.bindString(4, FDeptID);
        }
 
        String FOrg = entity.getFOrg();
        if (FOrg != null) {
            stmt.bindString(5, FOrg);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Buyer readEntity(Cursor cursor, int offset) {
        Buyer entity = new Buyer( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // FID
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // FNumber
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // FName
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // FDeptID
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // FOrg
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Buyer entity, int offset) {
        entity.setFID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setFNumber(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFDeptID(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFOrg(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Buyer entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Buyer entity) {
        return null;
    }

    @Override
    public boolean hasKey(Buyer entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
